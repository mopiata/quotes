import { HighlightbestDirective } from './highlightbest.directive';

describe('HighlightbestDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightbestDirective();
    expect(directive).toBeTruthy();
  });
});
